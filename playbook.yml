---
- name: Pull docker image of Calculator
  hosts: all
  tasks:
    - name: Start docker service
      service:
        name: docker
        state: started

    - name: pull docker image
      shell: docker pull octopops24/calculator

    - name: running container
      shell: docker run -it -d octopops24/calculator /bin/bash
